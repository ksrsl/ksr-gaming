name: Deploy KSR Gaming to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: ksr-gaming-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out KSR Gaming
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload KSR Gaming site
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs

      - name: Publish KSR Gaming
        id: deployment
        uses: actions/deploy-pages@v4
